# Cloudflare Pages redirects - NO INFINITE LOOPS
# https://developers.cloudflare.com/pages/configuration/redirects/

# Exact path redirects (no infinite loops)
/studio /studio/ 200
/admin /admin/ 200

# Brand routes with Chinese character support
/brands/:slug /brands/:slug/ 200

# API routes (if any)
/api/* /api/:splat 200

# Default fallback - catch-all for SPA routing
/* /index.html 200