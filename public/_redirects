# Cloudflare Pages redirects for SPA routing
# https://developers.cloudflare.com/pages/configuration/redirects/

# CRITICAL: Studio routes MUST be first to prevent wildcard conflicts
# Studio routes - serve the studio page for all studio paths
/studio/ /studio/index.html 200
/studio/* /studio/index.html 200

# Admin panel routes - serve the admin page for all admin paths
/admin/ /admin/index.html 200
/admin/* /admin/index.html 200

# Brand routes with Chinese character support
/brands/* /brands/:slug/index.html 200

# API routes (if any)
/api/* /api/:splat 200

# Default SPA fallback - serve index.html for unmatched routes
# IMPORTANT: This wildcard rule must be LAST to avoid conflicts
/* /index.html 200