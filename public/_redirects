# Cloudflare Pages redirects - REMOVE INFINITE LOOP RULES
# https://developers.cloudflare.com/pages/configuration/redirects/

# Exact path redirects (no infinite loops)
/studio /studio/ 200
/admin /admin/ 200

# Brand routes with Chinese character support
/brands/:slug /brands/:slug/ 200

# API routes (if any)
/api/* /api/:splat 200

# DO NOT USE CATCH-ALL REDIRECT - CAUSES INFINITE LOOPS
# Cloudflare Pages handles SPA routing automatically